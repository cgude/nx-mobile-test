import React from 'react';
import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface SrcProps {
}

export function Src(props: SrcProps) {
  return (
    <View>
      <Text>Welcome to src!</Text>
    </View>
  );
}

export default Src;
